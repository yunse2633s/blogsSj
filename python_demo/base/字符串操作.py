#!/usr/lib/python
#
#字符串操作
#判断字符串类型
#各类型数据的读取
#字段间转换
#字符值替换
#时间戳
#字符串截取
#'r'是防止字符转义的 value=r'G:/work/vod/axis/right'
# if else for in : 先for in ，再 if else
# 如 value = 1 if i>b else 2 for i in array 意思是 从array 中遍历初i 与b 对比赋值。
# 